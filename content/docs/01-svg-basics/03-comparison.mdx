---
title: '1.3 SVG 与其他图形技术对比'
description: '在 Canvas、WebGL、CSS 和各种图片格式之间，何时应该选择 SVG？'
icon: 'GitCompare'
---

import { Callout } from 'fumadocs-ui/components/callout';
import { SvgVsCanvas } from '@/components/docs/01-svg-basics/comparison/SvgVsCanvas';
import { TechComparison } from '@/components/docs/01-svg-basics/comparison/TechComparison';

在前端开发中，我们有许多图形绘制技术可供选择。了解它们的差异，是做出正确架构决策的前提。

## 1. 核心对决：SVG vs. Canvas

这是前端面试中最经典的对比题。核心差异在于：**SVG 是基于 DOM 的声明式绘图，而 Canvas 是基于像素的命令式绘图**。

<SvgVsCanvas />

## 2. 更多维度的技术对比

除了 Canvas 之外，前端工程师在日常开发中还经常需要在 SVG 与 WebGL、纯 CSS 绘制以及传统光栅图像格式之间做出选择。

<TechComparison />
