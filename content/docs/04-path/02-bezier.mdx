---
title: '4.3 贝塞尔曲线'
description: '彻底搞懂二次贝塞尔 (Q) 和三次贝塞尔 (C) 曲线的控制原理'
icon: 'Spline'
---

import { Callout } from 'fumadocs-ui/components/callout';
import { BezierPlayground } from '@/components/docs/04-path/bezier/BezierPlayground';
import { BezierAnatomy } from '@/components/docs/04-path/bezier/BezierAnatomy';

直线只能画出棱角分明的图形，要画出圆润、有机的形状，我们需要**贝塞尔曲线（Bézier Curve）**。

在 SVG 路径中，最常用的是**二次贝塞尔曲线（Q）**和**三次贝塞尔曲线（C）**。

## 交互探索：拖拽控制点

这是学习贝塞尔曲线最好的方法：不要看公式，直接**动手拖拽画布上的点**，感受“控制点”是如何像磁铁一样拉扯曲线的。

<BezierPlayground />

---

## 指令解析

下面我们通过静态图解，深入分析这两种曲线的语法结构和控制点作用。

<BezierAnatomy />