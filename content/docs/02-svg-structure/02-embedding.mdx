---
title: '2.2 SVG 嵌入方式大全'
description: '在网页中使用 SVG 的 5 种姿势及其能力对比'
icon: 'Plug'
---

import { Callout } from 'fumadocs-ui/components/callout';
import { EmbeddingMethodsLab } from '@/components/docs/02-svg-structure/embedding/EmbeddingMethodsLab';
import { EmbeddingComparison } from '@/components/docs/02-svg-structure/embedding/EmbeddingComparison';

在网页中显示图片，通常只有 `<img>` 标签或 CSS `background-image` 两种选择。但由于 SVG 既是图像又是文档（DOM），它在网页中的嵌入方式多达 5 种以上。

选择哪种嵌入方式，完全取决于你的**使用场景**（是否需要动态改颜色？是否需要缓存？是否包含复杂交互？）。

## 嵌入方式能力实验室

请通过下方的交互面板，亲手测试不同嵌入方式下，**外部 CSS** 和 **JavaScript** 对 SVG 的控制能力。

<EmbeddingMethodsLab />

---

## 详细对比与最佳实践

<EmbeddingComparison />

<Callout type="tip" title="CSS 遮罩黑科技 (Mask)">
  如果你既想享受外链文件的缓存优势，又想通过 CSS 动态改变单色图标的颜色，可以使用 `mask` 属性：<br/>
  `background-color: blue; mask: url('icon.svg');`
</Callout>