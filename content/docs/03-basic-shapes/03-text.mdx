---
title: '3.5 文本排版'
description: '在 SVG 中绘制文字：基础对齐、多行处理与炫酷的路径文字'
icon: 'Type'
---

import { Callout } from 'fumadocs-ui/components/callout';
import { TextPlayground } from '@/components/docs/03-basic-shapes/text/TextPlayground';
import { TextCapabilitiesExplorer } from '@/components/docs/03-basic-shapes/text/TextCapabilitiesExplorer';

与 HTML 中的 `<p>` 或 `<span>` 不同，SVG 的 `<text>` 元素有着完全不同的排版逻辑。它更像是图形编辑器（如 Photoshop 或 Illustrator）中的文本工具。

## 交互探索

体验 SVG 文本的三大核心能力：**基础对齐**、**嵌套样式 (tspan)** 以及炫酷的**沿路径排版 (textPath)**。

<TextPlayground />

---

## 文本排版的核心概念

下面我们通过交互式图解，深入理解 SVG 文本排版的三个核心特性：对齐方式、换行策略和路径文字。

<TextCapabilitiesExplorer />