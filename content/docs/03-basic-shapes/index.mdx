---
title: '3.1 & 3.2 线条与矩形'
description: '掌握绘制直线 (line)、折线 (polyline) 和矩形 (rect) 的基本属性'
icon: 'Minus'
---

import { Callout } from 'fumadocs-ui/components/callout';
import { LineRectPlayground } from '@/components/docs/03-basic-shapes/lines-rects/LineRectPlayground';
import { LineRectAnatomy } from '@/components/docs/03-basic-shapes/lines-rects/LineRectAnatomy';

虽然 SVG 的终极武器是 `<path>`（路径），但对于一些规则的几何图形，使用语义化的基础形状元素会更简单、代码更易读。

本节我们将学习最基础的三个形状：`<line>`（直线）、`<polyline>`（折线）和 `<rect>`（矩形）。

## 交互探索

请通过下方的滑块，动手改变属性值，观察右侧图形的实时变化。

<LineRectPlayground />

---

## 重点解析

下面我们将这三种基础形状的核心属性进行了图解化整理。

<LineRectAnatomy />