---
title: '5.1 & 5.2 样式基础与填充'
description: '了解给 SVG 添加样式的三种方式，并掌握填充 (Fill) 与 currentColor 的黑科技'
icon: 'PaintBucket'
---

import { Callout } from 'fumadocs-ui/components/callout';
import { FillStylePlayground } from '@/components/docs/05-style/fill/FillStylePlayground';
import { FillConceptsExplorer } from '@/components/docs/05-style/fill/FillConceptsExplorer';

画出了形状和路径后，我们需要给它们上色。SVG 提供了极其丰富的样式控制能力。

## 添加样式的三种方式

SVG 是一个神奇的规范，它允许你用三种不同的方式为同一个属性赋值（比如填充颜色）。它们的优先级从低到高如下：

1. **表现属性 (Presentation Attributes)**：最常见的方式。直接写在标签上，如 `<rect fill="red" />`。它优先级最低，很容易被外部 CSS 覆盖。
2. **内部/外部 CSS 样式表**：你可以通过 `class` 类名，用普通的 CSS 给 SVG 上色：`.my-rect { fill: blue; }`。
3. **内联 style 属性**：优先级最高。`<rect style="fill: green;" />`。

<Callout type="tip" title="最佳实践">
  在现代前端开发中，推荐使用**表现属性**（如 <code>fill="red"</code>）来设置默认样式，这样你可以随时在外部用 CSS（如 Tailwind 的 <code>fill-blue-500</code> 类）轻松覆盖它。
</Callout>

---

## 交互探索：填充与透明度

在下方的演示器中，尝试改变填充颜色、对比 `fill-opacity` 与全局 `opacity` 的区别，并体验强大的 `currentColor` 魔法。

<FillStylePlayground />

## 核心概念解析

SVG 的填充系统包含了一些非常核心且实用的概念，例如不同的填充颜色表示、`none` 与透明的区别，以及被广泛应用于图标系统的 `currentColor`。

<FillConceptsExplorer />
